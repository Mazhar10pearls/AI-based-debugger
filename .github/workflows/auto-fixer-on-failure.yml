name: Auto Fix Failed Local CI

permissions:
  contents: write
  actions: write
  pull-requests: write
  issues: write

on:
  workflow_run:
    workflows:
      - Local CI/CD Deploy
    types:
      - completed

# ── Serialize inference on the single 8GB box ───────────────────────────────
# Only one auto-fix (= one Ollama inference) may run at a time. cancel-in-progress
# is false so a fix already mid-flight is never killed by a newer failure.
# NOTE: this box has 2 runners on 8GB. If a main-CI build and an auto-fix ever
# overlap you risk OOM — either run a single runner here, or set
# OLLAMA_NUM_PARALLEL=1 / OLLAMA_MAX_LOADED_MODELS=1 on the Ollama systemd unit.
concurrency:
  group: ollama-auto-fix
  cancel-in-progress: false

jobs:
  auto-fix:
    # Run only if CI failed AND the failing commit was NOT the bot (loop guard).
    if: |
      github.event.workflow_run.conclusion == 'failure' &&
      github.event.workflow_run.head_commit.author.name != 'github-actions[bot]'

    name: Auto-Fix Failed Workflow
    runs-on: [self-hosted, linux, ai-runner]
    environment: prod
    # 30-min budget:
    #   ~2 min  checkout + pip + git setup + log download
    #   ~2 min  repo discovery + scoring
    #   ~7 min  Ollama inference (2 attempts × 210s + 20s backoff)
    #   ~2 min  validate + write + git + PR
    #   ─────────────────────────────────────────────
    #   ~13 min expected | 30 min hard ceiling
    timeout-minutes: 30

    steps:
      # ── Resolve the branch that actually failed (branch-aware git flow) ───
      # The fix must branch off, and PR back to, the SAME branch the failure
      # happened on — not a hardcoded 'develop'. A main failure → fix PR to
      # main; a develop failure → fix PR to develop.
      - name: Resolve originating branch
        id: src
        run: |
          BRANCH="${{ github.event.workflow_run.head_branch }}"
          if [ -z "$BRANCH" ]; then
            echo "No head_branch on the event — cannot proceed."
            exit 1
          fi
          echo "branch=$BRANCH" >> "$GITHUB_OUTPUT"
          echo "Originating (failed) branch: $BRANCH"

      - name: Checkout originating branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ steps.src.outputs.branch }}
          token: ${{ secrets.GH_PAT }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests pyyaml --quiet

      - name: Validate required secrets
        run: |
          : "${OLLAMA_API_URL:?Secret OLLAMA_API_URL is not set}"
          : "${OLLAMA_MODEL:?Secret OLLAMA_MODEL is not set}"
          : "${GH_PAT:?Secret GH_PAT is not set}"
        env:
          OLLAMA_API_URL: ${{ secrets.OLLAMA_API_URL }}
          OLLAMA_MODEL:   ${{ secrets.OLLAMA_MODEL }}
          GH_PAT:         ${{ secrets.GH_PAT }}

      - name: Download failed workflow logs
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          python3 - <<'PY'
          import json, os, pathlib, requests, zipfile, sys

          with open(os.environ['GITHUB_EVENT_PATH'], 'r', encoding='utf-8') as f:
              event = json.load(f)

          run_id    = event['workflow_run']['id']
          repo_full = event['repository']['full_name']
          headers   = {
              'Authorization': f"Bearer {os.environ['GH_PAT']}",
              'Accept': 'application/vnd.github+json'
          }
          url  = f'https://api.github.com/repos/{repo_full}/actions/runs/{run_id}/logs'
          resp = requests.get(url, headers=headers, timeout=120)
          if resp.status_code != 200:
              print(f'Failed to download logs: HTTP {resp.status_code}', file=sys.stderr)
              sys.exit(1)

          with open('workflow_logs.zip', 'wb') as fh:
              fh.write(resp.content)

          extract_dir = pathlib.Path('logs')
          extract_dir.mkdir(parents=True, exist_ok=True)
          with zipfile.ZipFile('workflow_logs.zip', 'r') as zf:
              zf.extractall(extract_dir)

          out_path = pathlib.Path('failure.log')
          with out_path.open('w', encoding='utf-8') as out:
              for path in sorted(extract_dir.rglob('*.txt')):
                  out.write(f'=== {path.relative_to(extract_dir)} ===\n')
                  out.write(path.read_text(encoding='utf-8', errors='replace'))
                  out.write('\n\n')

          print(f'Generated failure.log ({out_path.stat().st_size} bytes)')
          PY

      - name: Check failure log is non-empty
        run: |
          if [ ! -s failure.log ]; then
            echo "failure.log is empty — nothing to fix."
            exit 1
          fi

      - name: Configure git with PAT remote
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin \
            "https://x-access-token:${{ secrets.GH_PAT }}@github.com/${{ github.repository }}.git"

      # ── Smoke-test: Ollama reachable before spending time on logs ─────────
      - name: Verify Ollama is reachable
        env:
          OLLAMA_API_URL: ${{ secrets.OLLAMA_API_URL }}
        run: |
          BASE_URL=$(echo "$OLLAMA_API_URL" \
            | sed 's|/v1/completions||; s|/v1/chat/completions||; s|/api/generate||; s|/*$||')
          echo "Checking Ollama at: $BASE_URL"
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 "$BASE_URL" || echo "000")
          if [ "$HTTP_CODE" = "000" ]; then
            echo "❌ Ollama unreachable at $BASE_URL — aborting to save budget"
            exit 1
          fi
          echo "✓ Ollama responded (HTTP $HTTP_CODE)"

      - name: Run auto-fixer
        env:
          OLLAMA_API_URL_RAW: ${{ secrets.OLLAMA_API_URL }}
          OLLAMA_MODEL:       ${{ secrets.OLLAMA_MODEL }}
          GITHUB_TOKEN:       ${{ secrets.GH_PAT }}
          GITHUB_REPOSITORY:  ${{ github.repository }}
          # Branch-aware: base AND target are the branch that failed.
          GIT_BASE_BRANCH:    ${{ steps.src.outputs.branch }}
          GIT_TARGET_BRANCH:  ${{ steps.src.outputs.branch }}
        run: |
          # Force the native /api/generate endpoint so the JSON-schema `format`
          # constraint applies (it is flaky on /v1/completions). Works no matter
          # how the OLLAMA_API_URL secret is written.
          BASE=$(echo "$OLLAMA_API_URL_RAW" \
            | sed 's|/v1/completions||; s|/v1/chat/completions||; s|/api/generate||; s|/*$||')
          export OLLAMA_API_URL="$BASE/api/generate"
          echo "Using Ollama endpoint: $OLLAMA_API_URL"
          echo "Git flow → fix/* off '$GIT_BASE_BRANCH', PR targets '$GIT_TARGET_BRANCH'"
          python3 auto-fixer.py --input failure.log --skip-tests

      # ── If the fixer could not fix it → open an Issue ─────────────────────
      - name: Open issue if fixer failed
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          gh issue create \
            --title "🚨 Auto-fixer could not fix CI failure on '${{ steps.src.outputs.branch }}' — manual review" \
            --body "The auto-fixer ran but could not produce a valid patch.

          **Failed branch:** \`${{ steps.src.outputs.branch }}\`
          **Failed run:** ${{ github.event.workflow_run.html_url }}

          **Git flow reminder:**
          - Fix manually on the branch above.
          - Promote via: develop → release/* → main (+ tag) after QA." \
            --label "bug" || true